# Base URL of the API. Leave unset in local dev to use the Vite proxy (/api -> localhost:3001).
# In production (e.g. Vercel), set to your deployed API URL, e.g. https://your-api.vercel.app
# API_URL=https://your-api.vercel.app

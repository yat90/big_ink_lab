# MongoDB connection string. Defaults to mongodb://localhost:27017/big-ink-lab if unset.
# MONGODB_URI=mongodb://localhost:27017/big-ink-lab

# Anthropic API key — enables lore counter paper scanning via Claude Vision.
# Without this key the /matches/scan-lore-counter endpoint returns available: false.
# ANTHROPIC_API_KEY=sk-ant-...

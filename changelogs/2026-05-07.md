# Changelog — 2026-05-07

Scope: **`git log --since="3 days ago"`** (as of 2026-05-07). All commits in that window land on **2026-05-07** only: **`6339369`**, **`a454401`**, **`b49f547`**.

The format is inspired by [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [Unreleased]

### API

- **Team overview** (`apps/api/src/team/team.controller.ts`): Response adds **`openAccusationsCount`** (alongside balance / internal ranking); **`TeamAccusationsService.countOpen(team)`** counts open accusations (`apps/api/src/team/team-accusations.service.ts`). **`TeamOverview`** extended with **`openAccusationsCount`** (`apps/api/src/team/interfaces/team-overview.interface.ts`).

### Added (web)

- **App-wide breadcrumb:** **`AppBreadcrumb`**, **`breadcrumbTrail.ts`**, **`breadcrumbTail`** for dynamic last crumb (e.g. tournament name), **`Breadcrumb.svelte`**; wired in root **`+layout.svelte`**. Shared **`.page-title` / `.page-sub`** in **`app.css`**.
- **Team:** **`links`** tab with **`TeamTabLinks.svelte`** and **`teamDriveLinks.ts`** (Google Drive: Logos, Wappen, Merch, Meetings); overview quick card links the **Meetings** folder. Team tab files renamed to **`TeamTab*.svelte`** (`TeamTabMembers`, `TeamTabRanking`, …).
- **Navigation:** new icons **`IconChevronLeft`**, **`IconChevronRight`**, **`IconCircleUser`**; updated desktop / mobile nav, team submenu, and players link.

### Changed (web)

- **`apps/web/src/lib/team.ts`:** overview type includes **`openAccusationsCount`**.
- **`routes/team/+page.svelte`:** team header shows **open accusations** metric (jumps to **`?tab=court`**); layout/tabs updated for new team tabs and Drive links.
- **`DesktopNavBar.svelte` / `MobileNavDrawer.svelte`:** dropdowns/drawer refresh; team tab targets include **`links`** and **`IconCloud`**.
- **Stats & players:** **`PlayerStatsOverview`**, **`MatchupStatistics`**, **`Stats*Tab`**, **`routes/me/statistics/*`**, **`routes/stats/+page.svelte`**, **`routes/players/[id]/+page.svelte`** — **`$t` / `translate`** and new **`en.ts` / `de.ts`** copy.
- **Tournaments:** **`tournaments/+page.svelte`**, **`[id]`**, **`new`**, **`results`** — shared heading styles, i18n, duplicate breadcrumbs removed.
- **`routes/matches/new/+page.svelte`:** page-level title styles removed (uses global classes).

### Notes

- **Commits:** `6339369` (team overview + nav), `a454401` (i18n, breadcrumb, tournaments/stats), `b49f547` (Drive links tab, `TeamTab*` filenames).
- Root **`CHANGELOG.md`:** index row added for this date.
